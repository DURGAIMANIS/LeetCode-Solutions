        double max=sum;
        for(int i=k;i<nums.length;i++){
            sum+=nums[i];
        }
            sum-=nums[i-k];
            max=Math.max(max,sum);
        return max/k;
        }
            sum+=nums[i];
        for(int i=0;i<k;i++){
    }
        double sum=0;
    public double findMaxAverage(int[] nums,int k) {
class Solution {
}
