            maxsum=Math.max(maxsum,currsum);
            //but if the negetive comes, then  make it zero
            if(currsum<0){
                currsum=0;
            }    
        }
        return maxsum;

        //Keep adding elements. If the current sum becomes worse than starting fresh, start a new 
        subarray.
        

    }
}
