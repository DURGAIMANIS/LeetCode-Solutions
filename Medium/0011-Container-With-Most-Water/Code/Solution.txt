        int maxarea=0;
        while(left<right){

        }
            int h=Math.min(height[left],height[right]);//min decide how much store the water
            int b=right-left; //right>left

            int area=h*b;
            maxarea=Math.max(maxarea,area);

            if(height[left]<height[right]) left++;
            else right--;

        int right=height.length-1;
        int left=0;
    public int maxArea(int[] height) {
class Solution {
