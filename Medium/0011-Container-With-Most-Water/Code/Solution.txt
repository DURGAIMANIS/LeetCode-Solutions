            //it store the water in reactangular box,so we need lenght*base;
            int length=Math.min(height[left],height[right]);
            int base=right-left; //8-0 =8
            int area=length*base;
            maxArea=Math.max(maxArea,area);

            if(height[left]<=height[right]) left++;//i need max water, so we need to hold greater value always
            else right--;
        }

        return maxArea;
    }
        while(left<right){

        int maxArea=0;

        int right=height.length-1;
        int left=0;
