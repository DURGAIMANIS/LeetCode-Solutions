                currentEnd=nextEnd;
            }
        }

        result[resultIndex][0]=currentStart;//last set
        result[resultIndex][1]=currentEnd;

        resultIndex++;
        return Arrays.copyOf(result,resultIndex);
    }
}
