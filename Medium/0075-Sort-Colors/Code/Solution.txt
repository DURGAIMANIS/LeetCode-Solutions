            nums[low]=0;
            low++;
            mid++;
            nums[mid]=nums[low];
            if(nums[mid]==0){//if it is 0
        while(mid<=high){

            }else if(nums[mid]==1){//if it is 1
               mid++;
            }else{//if it is 2
        //mid to high-1 for 2s
        //low to mid-1 for 1s
        //0 to low-1 for 0s
        int high=n-1;
        int mid=0;
        int low=0;
        int n=nums.length;
        
