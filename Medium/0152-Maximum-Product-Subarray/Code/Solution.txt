        for(int i=1;i<nums.length;i++){
            if(nums[i]<0){
                int temp=max;
                max=min;
                min=temp;
            }

            max=Math.max(nums[i],max*nums[i]);//stores max
            min=Math.min(nums[i],min*nums[i]);//stores min
            answer=Math.max(answer,max);
        }
        return answer;

        int answer=nums[0];
        int min=nums[0];
        int max=nums[0];
