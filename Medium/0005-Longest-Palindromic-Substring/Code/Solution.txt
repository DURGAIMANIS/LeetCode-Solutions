        return answer;

    }
        }

            String even=expend(s,i,i+1);

            String odd=expend(s,i,i);//odd char
            if(odd.length()>answer.length()){
                answer=odd;
            }
            if(even.length()>answer.length()){
                answer=even;
            }

        for(int i=0;i<s.length();i++){
        String answer="";
