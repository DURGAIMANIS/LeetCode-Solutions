            count+=expend(s,i,i+1);

        }
        return count;

    }
    public int expend(String s,int left,int right){
        int count=0;
        while(left>=0&&right<s.length()&&s.charAt(left)==s.charAt(right)){
            count++;
            count+=expend(s,i,i);//odd char
        for(int i=0;i<s.length();i++){

        int count=0;

            left--;
            right++;
        }
