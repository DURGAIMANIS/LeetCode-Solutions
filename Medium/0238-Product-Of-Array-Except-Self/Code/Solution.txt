
        // Product of all elements to the right
        int after = 1;

        for (int i = nums.length - 1; i >= 0; i--) {
            arr[i] *= after;
            after *= nums[i];
        }

        return arr;
        
    }
        }
            before *= nums[i];
            arr[i] = before;
        for (int i = 0; i < nums.length; i++) {

        int before = 1;
